'use client'
import { Suspense } from 'react';import Link from 'next/link';import { useSearchParams } from 'next/navigation';import GoogleSignInButton from '@/app/components/GoogleSignInButton';import { SignUpForm } from '@/app/components/EmailAuthForms';import { safeNextPath } from '@/lib/auth/next-path';import styles from '@/app/auth.module.css'
function Content(){const next=safeNextPath(useSearchParams().get('next'));return <main className={styles.page}><section className={styles.card}><Link href="/" className={styles.brand}>openly<span>.</span></Link><h1>Create your account</h1><p className={styles.intro}>Meet your court crew and reserve your next game.</p><SignUpForm nextPath={next}/><div className={styles.divider}>or</div><div className={styles.google}><GoogleSignInButton nextPath={next} label="Sign up with Google"/></div><p className={styles.footer}>Already have an account? <Link href={`/login?next=${encodeURIComponent(next)}`}>Log in</Link></p></section></main>}export default function Page(){return <Suspense fallback={null}><Content/></Suspense>}
